// it is resposiable for the changes in the html pages in the website

// it is take the html and break the pages and store a such a way to store that give the pages that the user nees

// with the doms only the html page can be make it dynamaic

// model :: the structure that handle the data

const titel =document.getElementById("heading");
titel.innerText = "hello from javascript!";
